# Green Baseline (2026-02-03)

Baseline target commit (tests last confirmed): `339d97b`.

## Environment
- OS: Windows (PowerShell)
- Docker Compose: infra/docker-compose.yml
- API port: 3000

## Commands (verified)
```
docker compose -f infra/docker-compose.yml up -d --build
$env:API_BASE_URL="http://localhost:3000"
$env:API_ADMIN_EMAIL="admin@example.com"
$env:API_ADMIN_PASSWORD="admin1234"
$env:API_HEALTH_TIMEOUT_MS="5000"
npm test --workspace @livestock/api -- test/api.integration.test.ts
npm run smoke --workspace @livestock/api
npm test --workspaces
```

## Results
- API integration tests: PASS (4/4)
- Smoke test: PASS
- Workspace tests: PASS (api 7/7, shared 2/2)
- DB health test: SKIPPED (DATABASE_URL missing)
