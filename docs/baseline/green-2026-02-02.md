# Green Baseline (2026-02-02)

Baseline target commit (tests last confirmed): `562659b`.
This record commit only adds baseline metadata and ignore rules.

## Environment
- OS: Windows (PowerShell)
- Docker Compose: infra/docker-compose.yml
- API port: 3000

## Commands (verified)
```
docker compose -f infra/docker-compose.yml up -d --build --force-recreate api
$env:API_BASE_URL="http://localhost:3000"
$env:API_ADMIN_EMAIL="admin@example.com"
$env:API_ADMIN_PASSWORD="admin1234"
npm test --workspace @livestock/api -- test/api.integration.test.ts
```

## Results
- API integration tests: PASS (4/4)
- Refresh rotation test: PASS
- Health/metrics: OK
